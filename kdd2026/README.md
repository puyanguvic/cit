# kdd2026 submission snapshot

This folder contains a self-contained snapshot of the current submission materials:

- `paper.tex`, `appendix.tex`
- `Figs/` (all referenced figures)
- `Tables/` (all referenced tables)

The `.tex` files in this folder are patched to use local `Figs/` and `Tables/` paths.
