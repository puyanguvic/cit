[project]
name = "cit"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pandas",
  "scikit-learn",
  "torch",
  "tqdm",
  "tokenizers",
  "transformers",
  "datasets",
  "matplotlib",
  "openml",
]

[tool.setuptools.packages.find]
where = ["src"]
