# Python bytecode
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/
ENV/
env/

# Build artifacts
build/
dist/
*.egg-info/

# Experiment outputs / logs
# Track paper/curated outputs under results/, but ignore heavyweight caches.
results/hf_cache/

# JSONL logs (tracked under results/, ignored elsewhere)
*.jsonl
!results/**/*.jsonl
*.pt
*.pth
*.ckpt

# Data downloads (keep small curated CSVs if committed)
# Paper datasets are not tracked; scripts can auto-download as needed.
data/csic2010/
